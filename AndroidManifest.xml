<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sim.weddingmanager"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="19" />

    <!-- TRAP 3 -->
    <application
        android:name="com.sim.weddingmanager.MainApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- TRAP 0 -->
        <activity
            android:name="com.sim.weddingmanager.MainActivity"
            android:label="@string/app_name" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.sim.weddingmanager.MainActivity" />
        </activity>
        <activity
            android:name="com.sim.weddingmanager.DashboardActivity"
            android:label="@string/title_activity_dashboard"
            android:theme="@android:style/Theme.Holo.Light" >
        </activity>
        <activity
            android:name="com.sim.weddingmanager.BillingActivity"
            android:label="@string/title_activity_billing" >
        </activity>
        <activity android:name="com.sim.weddingmanager.EventListActivity" >
        </activity>
        <activity
            android:name="com.sim.weddingmanager.SplashActivity"
            android:label="@string/title_activity_splash"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>